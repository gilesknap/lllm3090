"""Data files shipped with the package (the model catalogue).

This is a package rather than a bare directory so ``importlib.resources`` can
find its contents in a wheel as well as in a source checkout.
"""
